{{#each data}}
<div class="container">
    <h1>{{name}}</h1>
    {{#if imagepath}}
    <img src="{{imagepath}}" alt="{{name}}" class="float-start me-3" />
    {{/if}}
    <p class="text-black-50">{{{description}}}</p>
    {{/each}}