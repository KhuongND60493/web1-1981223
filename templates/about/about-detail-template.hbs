<section class="container py-5">
    {{#each data}}
    <h1 class="py-3">{{name}}</h1>
    <div class="row">
        <div class="col-lg-4 col-md-6 col-sm-12">
            <img src="{{imagepath}}" alt="{{name}}" class="float-start rounded-4 me-3 img-fluid" />
        </div>
        <div class="col-lg-8 col-md-6 col-sm-12">
            {{{description}}}
        </div>
    </div>
    {{/each}}
</section>